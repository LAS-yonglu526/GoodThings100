# 好事100 — 项目交接状态文档

> 最后更新: 2026-06-06 21:37 (Asia/Shanghai)
> Expo SDK 54 / React Native 0.81 / TypeScript 5.9

---

## 1. 项目愿景与架构

### 项目定位
《好事100》(GoodThings100) 是一款基于 React Native + Expo 的精美数字清单 App。核心理念是用**果冻毛玻璃**视觉语言记录和追踪日常好事。

### 技术栈
| 层级 | 技术 |
|------|------|
| 框架 | React Native 0.81 + Expo Managed Workflow (SDK 54) |
| UI 组件 | 纯 React Native + `expo-blur` (iOS 原生毛玻璃) |
| 动画引擎 | `Animated.Value/ValueXY` + `PanResponder` + `LayoutAnimation` |
| 数据库 | SQLite (`expo-sqlite`, 单文件 `goodthings.db`) |
| 触觉反馈 | `expo-haptics` |
| 图片 | `expo-image-picker` + `expo-file-system/legacy` + `expo-image-manipulator` |
| 生物识别 | `expo-local-authentication` (Face ID) |
| 安全存储 | `expo-secure-store` (账号列表/密码标记/session token) |
| 分享截图 | `react-native-view-shot` + `expo-sharing` |
| 后端 | Supabase (Email OTP + 密码登录) |

### 文件结构
```
d:\GoodThings100\
├── App.tsx                           # overlay 架构根导航 (none/detail/settings/timeline)
├── HANDOFF.md
├── start-tunnel.js
├── package.json
├── tsconfig.json
└── src/
    ├── config/
    │   └── supabase.ts               # Supabase client (AsyncStorage session)
    ├── services/
    │   ├── database.ts               # SQLite (lists + good_items, isShared字段)
    │   ├── imageStorage.ts           # 图片选择/压缩 + pickAndCompressAvatar
    │   ├── templates.ts              # 8套主题模板
    │   ├── auth.ts                   # OTP/密码登录/注册/多账号/Profiles同步
    │   └── couple.ts                 # 双人模式服务 (邀请码/结对/共享清单/回忆墙/Realtime)
    ├── screens/
    │   ├── ListHomeScreen.tsx        # 首页: 清单网格 + 伴侣共享清单区 + Face ID
    │   ├── ListDetailScreen.tsx      # 详情: 胶囊列表 + Realtime订阅 + 伴侣动态Toast
    │   ├── SettingsScreen.tsx        # 设置: 5模式登录/注册/安全信息/Face ID/头像/伴侣
    │   └── SharedTimelineScreen.tsx  # 🆕 共同回忆时间线
    └── components/
        ├── MemoryModal.tsx           # 手记编辑 (保存时推送到shared_memories)
        ├── AddItemOverlay.tsx
        ├── ShareCard.tsx
        └── ShareModal.tsx
```

---

## 2. 绝对红线与开发禁忌

### 2.1 动画引擎底线
```
🚫 绝对禁止 react-native-reanimated
   原因: iOS Hermes 不支持 # 私有属性语法 → 红屏 NativeWorklets
```

### 2.2 useNativeDriver 防报错准则
| 动画属性 | useNativeDriver |
|----------|----------------|
| `transform`, `opacity` | **必须 `true`** |
| `top/left/width/height` | **必须 `false`** |

同一个 `Animated.Value` 不能混用两种驱动模式。

---

## 3. 启动方式
```bash
cd d:\GoodThings100
npx expo start -c      # LAN 模式（推荐）
node start-tunnel.js    # 自动化 bore 隧道调试
```

---

## 4. 🆕 登录系统 (v1.3 重新设计)

### 4.1 登录方式
| 方式 | 说明 |
|------|------|
| **验证码登录（主推）** | 输入邮箱 → 发6位OTP → 验证。新用户和重置密码均走此流程 |
| **密码登录** | 邮箱+密码。仅限已设密码的用户 |
| **快捷登录** | 已登录同号 → 直接进；不同号 → Alert 弹窗让用户选方式，**不发验证码** |
| **Face ID** | 设置页「安全」分区独立开关，开启后冷启动自动扫脸 |

### 4.2 注册流程
```
验证码登录 → 首次用户 → 弹窗「设置密码」→ 可跳过
→ 设置密码(确认) → 设置用户名(12中文/24英文上限) → 完成
```

### 4.3 安全信息
- 设置页显示「📧 邮箱已验证 ✅」
- 「🔐 密码保护」→ 已设密码显示「✅ 修改」点击可修改密码；未设密码显示「⚠️ 点击添加」
- 忘记密码：密码登录页底部「忘记密码」→ 发验证码 → 验证 → 直接设新密码

### 4.4 hasPassword 检测机制
三道防线：① `SecureStore` 本地 `gt100_has_pw_{uid}` 标记 ② `UserProfile.hasPassword` 缓存 ③ Supabase `profiles.has_password` 表

### 4.5 多账号
- `SecureStore` 存 `gt100_saved_accounts` (最多5个)，含 email/nickname/avatarEmoji/lastLogin
- 支持删除账号（二次确认）
- 密码输入框配置 `textContentType` / `autoComplete` 属性支持 iOS 密码自动填充

---

## 5. 🆕 双人模式 (Couple Mode)

### 5.1 技术架构
`src/services/couple.ts` 提供完整双人服务：
- **邀请码**：`generateInviteCode()` 生成长串 hex（如 `a3f2b901-d4e5-7c2a`），碰撞概率≈0，24h过期
- **结对绑定**：`createInvite` / `claimInvite` → Supabase `couples` 表
- **共享清单**：`pushSharedList` / `pushSharedItems` → Supabase `shared_lists` / `shared_items` 表
- **状态同步**：`pushItemStatusChange` + Realtime `subscribeSharedItems`
- **回忆墙**：`pushMemory` / `fetchMemories` / `subscribeMemories` → Supabase `shared_memories` 表

### 5.2 Realtime 实时通知
`ListDetailScreen` 订阅 Supabase Realtime channel，伴侣完成/删除胶囊时：
- 即时刷新本地列表
- 粉色毛玻璃 Toast：「💌 Ta 刚完成了「xxx」」+ 触觉反馈
- 4s 自动消失

### 5.3 伴侣 UI
- 首页「💕 Ta 的共享清单」粉色边框卡片区
- 详情页右侧「回忆」按钮 → `SharedTimelineScreen` 时间线
- 长按伴侣清单卡片 → 直接进回忆墙

---

## 6. Supabase 表结构

### 需手动建表
```sql
-- invites 邀请码表
CREATE TABLE invites (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, from_uid TEXT NOT NULL, code TEXT NOT NULL, expires_at TIMESTAMPTZ NOT NULL, claimed_by TEXT);

-- couples 结对关系表
CREATE TABLE couples (id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, partner1_uid TEXT NOT NULL, partner2_uid TEXT NOT NULL, status TEXT DEFAULT 'active');

-- shared_lists / shared_items / shared_memories (见 couple.ts 注释)

-- profiles 用户资料表
CREATE TABLE profiles (user_id TEXT PRIMARY KEY, nickname TEXT DEFAULT '', email TEXT DEFAULT '', avatar_emoji TEXT DEFAULT '👤', avatar_url TEXT DEFAULT '', has_password BOOLEAN DEFAULT false, updated_at TIMESTAMPTZ DEFAULT NOW());

-- Realtime
ALTER PUBLICATION supabase_realtime ADD TABLE shared_items;
ALTER PUBLICATION supabase_realtime ADD TABLE shared_memories;
```

### Supabase 后台配置
- Authentication → Settings → **关闭 Confirm email**（App 端 OTP 验证代替）
- Email Templates → 确保含 `{{ .Token }}`

---

## 7. 已知问题 & 待解决

| # | 问题 | 说明 |
|---|------|------|
| 1 | **profiles 表依赖** | 当前 `hasPassword` 检测优先用 SecureStore 本地标记 + 缓存，Supabase profiles 表查询仅做最后兜底。若 profiles 表未建，设置密码/用户名的数据无法云端持久化，仅存本地 |
| 2 | **快捷登录不能跨设备** | SecureStore 存的 session token 仅本机有效，换设备需重新验证码登录 |
| 3 | **双人模式未完整测试** | couples/shared_lists/shared_items/shared_memories 表需手动在 Supabase 创建，尚未端到端验证 |
| 4 | **expo-image-manipulator SDK 54 ESM** | `pickAndCompressAvatar` 中 import 已修复，但依赖 SDK 54 的 ESM 构建 |
| 5 | **Sign in with Apple 已移除** | 原占坑代码已从 SettingsScreen 删除，若需恢复见 git 历史 |
| 6 | **SecureStore 超 2048 字节** | Supabase session token 改回 AsyncStorage 存储（`supabase.ts` 已配置），SecureStore 仅用于小数据 |
| 7 | **Face ID 自动登录依赖历史账号** | 冷启动扫脸后自动读取 `savedAccounts[0]` 发 OTP，若账号列表为空则不触发 |
| 8 | **用户引导** | 新用户首屏默认进验证码登录，底部有「首次请用验证码登录」提示。密码登录页有「验证码登录」「忘记密码」「注册新账号」三个链接 |

---

## 8. 本次开发迭代记录 (2026-06-06)

### 新增依赖
- `expo-secure-store` — 账号列表/密码标记/头像本地存储
- `expo-local-authentication` — Face ID 生物识别
- `expo-image-manipulator` — 头像压缩 (150×150 JPEG 40%)

### 核心改动
| 文件 | 改动摘要 |
|------|---------|
| `auth.ts` | 完全重写：OTP/密码双登录, `SavedAccount` 多账号管理, `syncProfile`/`loadProfile` profiles同步, `setPasswordForUser` |
| `couple.ts` | 新建：邀请码/结对/共享清单/回忆墙/Realtime 全栈服务 |
| `database.ts` | `lists` 加 `isShared` 字段, `createSharedList`/`upsertItem`/`getSharedLists` |
| `supabase.ts` | SecureStore → AsyncStorage (session token 超限) |
| `imageStorage.ts` | `pickAndCompressAvatar` 头像选取+压缩 |
| `SettingsScreen.tsx` | 5模式登录面板, 安全信息区, emoji头像+照片上传, Face ID开关, 伴侣绑定, 用户名编辑 |
| `ListHomeScreen.tsx` | 伴侣共享清单区, 粉色边框卡片, 共享标签 |
| `ListDetailScreen.tsx` | Realtime 订阅, 伴侣动态Toast, 回忆按钮, 状态同步推送 |
| `SharedTimelineScreen.tsx` | 新建：共同回忆时间线 (毛玻璃卡片, 我/Ta区分) |
| `MemoryModal.tsx` | 保存时推送到 `shared_memories` |
| `App.tsx` | `timeline` overlay 路由, 伴侣清单拉取 |

### 踩坑记录
- `replace_in_file` 在中文字符/混合换行符下反复匹配失败 → 改 `write_to_file` 或 node 逐行替换
- `expo-image-manipulator` SDK 54 需 ESM `import * as`，不能用 `require()`
- `profiles` 表不存在导致 `loadProfile()` 返回 null → 密码提示死循环 → 加 `try-catch` + fallback
- `SecureStore` 存 Supabase session token 超 2048 字节限制 → 撤回 AsyncStorage
- `hasPassword` 用 state `userId` 在 `setShowLogin(false)` 后可能为 stale 值 → 改用 `getCurrentUserId()` 实时获取

---

**文档版本**: 8.0
**生成者**: Cline (Claude)

---

## 9. 迭代记录 2026-06-07 — ListDetailScreen 胶囊 UI 修复

### 修复清单
| # | 修复项 | 文件 | 说明 |
|---|--------|------|------|
| 1 | 手记提示图案位置 | `ListDetailScreen.tsx` | `✦` 从胶囊下方移至文字行内右侧（`pil.r` 内），补全 `pil.g`/`pil.gt` 样式（20×20, `#FF9AA2` 粉） |
| 2 | 选择模式 dim 策略 | `ListDetailScreen.tsx` | 整体 `opacity: 0.45` 移除，仅对 `<Text>` 做 dim，手记 `✦` 不再被隐藏 |
| 3 | 长按菜单 emoji | `ListDetailScreen.tsx` | `` 黑色占位符 → `📝` |
| 4 | 选择胶囊保留原色 + 边框高亮 | `ListDetailScreen.tsx` | 选中时 `backgroundColor: c+'DD'` + `borderColor: c` + `borderWidth: 2.5`，不再死红 |
| 5 | 返回按钮←居中 | `ListDetailScreen.tsx` | `st.bb` 加 `alignItems:'center'` + `justifyContent:'center'` |
| 6 | 100颗胶囊文字居中 | `ListDetailScreen.tsx` | `pil.p` 加 `justifyContent:'center'`，`pil.t` 保留 `textAlignVertical:'center'` |
| 7 | 呼吸效果匹配胶囊尺寸 | `ListDetailScreen.tsx` | glow 层改用 `layoutMapRef` 实测 `gw`/`gh` 替代估计算 |
| 8 | 防御规范加固 | `.clinerules/GOODTHINGS_RULES.md` | 新增「防御性编程铁律」「UI弹性与数值底线」「状态快照」三章 |
| 9 | Gallery 布局回归 | `ListDetailScreen.tsx` | 恢复 `galleryContainer: flexDirection:'column'`（防止退化） |

### 踩坑记录
- 外部脚本 `fix_all.js` 修改后导致代码严重退化（gallery 变 wrap、✦变📓、检查框出现、✕被删等），通过 `write_to_file` 完整覆盖恢复后叠加新修复
- `fontSize` 底线已从 `getFluidStyles` 确认：最小值 `Math.max(32, padV*2+fontSize+2)` → fontSize 最低 11（100颗时），但 `padV` 最低 4 已满足 ≥ 0，不再是硬编码
- 16:42 对话中断后文件被外部退化，19:51 读取 State Snapshot 全量复原
